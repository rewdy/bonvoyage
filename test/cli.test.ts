import { describe, expect, it } from "bun:test";
import chalk from "chalk";

describe("Test the cli", () => {
  it("should pass", () => {
    // Tests coming soon...
    console.log(chalk.bgMagenta(" Tests for the CLI will be added soon. "));

    expect(true).toBe(true);
  });
});
